

PreRequiste:

1) Install MongoDB.
2) Install nodejs.
3) Admin Rights.

SetUp:

1) UnZip the solution and go to home directory of project from command line.
2) Type following command in command Line from home
   sudo npm install (For package dependency) in mac system  or runas npm install(Window User)

3) Go to bin directory of mongodb where it installed and type followings commands into 2 different tabs

 -> ./mongod (db instance will run)
 -> ./mongo (mongo shell for query)

4)Run following command :-> ./scripts/web-server.sh (Server will start and Landing Page will show)

